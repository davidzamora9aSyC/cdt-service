services:
  cdt-service:
    image: node:20-bullseye
    container_name: cdt-service
    working_dir: /app
    command: sh -c "npm install && npx prisma generate && npm run start:dev"
    environment:
      NODE_ENV: development
      PORT: 3000
      DATABASE_URL: postgresql://root:rootpassword@dev_postgres:5432/main_db?schema=cdt
    ports:
      - "3004:3000"
    volumes:
      - ./:/app
      - cdt-service-node-modules:/app/node_modules
    networks:
      - ms-network

volumes:
  cdt-service-node-modules:

networks:
  ms-network:
    external: true
